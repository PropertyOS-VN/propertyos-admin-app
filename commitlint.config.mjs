/**
 * Conventional Commits config cho commitlint — enforce format
 * "type(scope?): subject" (VD: feat(building): them api xoa toa nha).
 */
export default {
  extends: ["@commitlint/config-conventional"],
};
